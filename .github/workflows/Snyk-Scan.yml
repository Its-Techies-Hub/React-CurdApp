name: workflow using Snyk

on: push

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Snyk test for vulnerabilities
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
         # args: --sarif-file-output=snyk.sarif # render results in the Security tab
          args: --json-file-output=snyk-results.json # render the result in json

      - name: Monitor the project with Snyk
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: monitor

      - name: Upload Snyk results to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk-results.json

      - name: Upload json file as artifact
        uses: actions/upload-artifact@v4
        with:
          name: snyk-report
          path: snyk-results.json
